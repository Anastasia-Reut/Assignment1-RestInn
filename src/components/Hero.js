import React from 'react';
import MainCarousel from './Carousel';
import '../assets/css/Hero.css';


const Hero = () => {
  return (
    <section id="hero">
      <MainCarousel/>
    </section>
  );
};

export default Hero;
